  package OOP_Interface;

public interface WHO // extends UNHG(not allowed)
// implements UNHG (not allowed)-- bcoz interface cannot have a parent class
{
	public void covidTest();
}
	
// An Interface can not have Parent class